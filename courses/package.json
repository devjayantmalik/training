{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "live-server public",
    "build": "rm -r public/main.css && export NODE_ENV=production && tailwindcss build -i src/main.css -o public/main.css --minify",
    "watch": "rm -r public/main.css && tailwindcss build -i src/main.css -o public/main.css --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1",
    "tailwindcss": "^2.2.4"
  }
}
